apiVersion: v1
kind: Namespace
metadata:
  name: arer
  labels:
    # Pod Security Admission: every workload here runs as non-root.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
